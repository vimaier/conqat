namespace X {

	class Y {
	
		private void AddCommssionSlidingScale(PricingSet set, SystemTime refDate, CommissionsForSet com, ICollection<IComission> slideList)
		{
		    if (com.IsActive) set.ActiveComission = slide;

		}
	
	}

}